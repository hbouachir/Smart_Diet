{{ form_start(form) }}

<div class="row">
    <div class="form-group col-sm-4 flex-column d-flex">
        <label class="form-control-label px-3">
            {{ form_label(form.nom) }}<span class="text-danger"> *</span></label>
        {{ form_errors(form.nom) }}
        {{ form_widget(form.nom) }}
    </div>
    <div class="form-group col-sm-4 flex-column d-flex">
        <label class="form-control-label px-3">
            {{ form_label(form.categorie) }}<span class="text-danger"> *</span></label>
        {{ form_errors(form.categorie) }}
        {{ form_widget(form.categorie) }}
    </div>

    <div class="form-group col-sm-4 flex-column d-flex">
        <label class="form-control-label px-3">
            {{ form_label(form.description) }}<span class="text-danger"> *</span></label>
        {{ form_errors(form.description) }}
        {{ form_widget(form.description) }}
    </div>


    <div class="form-group col-sm-4 flex-column d-flex">
        <label class="form-control-label px-3">
            {{ form_label(form.prix) }}<span class="text-danger"> *</span></label>
        {{ form_errors(form.prix) }}
        {{ form_widget(form.prix) }}
    </div>

    <div class="form-group col-sm-4 flex-column d-flex">
        <label class="form-control-label px-3">
            {{ form_label(form.image) }}<span class="text-danger"> *</span></label>
        {{ form_errors(form.image) }}
        {{ form_widget(form.image) }}
    </div>
</div>
<button class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

