<form method="post" action="{{ path('produit_delete', {'codeproduit': produit.codeproduit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.codeproduit) }}">
    <button class="btn">Delete</button>
</form>
